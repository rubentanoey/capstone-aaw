apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-wishlist
  namespace: marketplace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-wishlist
  template:
    metadata:
      labels:
        app: db-wishlist
    spec:
      containers:
        - name: postgres
          image: postgres:latest
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: wishlist-secret
                  key: DB_NAME
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: wishlist-secret
                  key: DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: wishlist-secret
                  key: DB_PASSWORD
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-wishlist-pvc
